<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        style="@style/root.ll_vertical">
    <LinearLayout style="@style/randomizer_buttons_bar"
            android:id="@+id/done_objectives_buttons_bar">
        <TextView style="@style/randomizer_score"
                android:id="@+id/done_objectives"
                android:text="@string/_done_objectives"/>
    </LinearLayout>
    <ListView android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/done_objectives_list">
    </ListView>
</LinearLayout>
